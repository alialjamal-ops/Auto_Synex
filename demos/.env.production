# The demos are proxied onto the main site at autosynex.com/demos, so every
# route, asset and link is generated under that prefix. Remove this file (or
# set an empty value) to serve them from the root instead.
NEXT_PUBLIC_BASE_PATH=/demos
